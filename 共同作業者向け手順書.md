# 結城塾LP 共同作業者向けセットアップ手順書

このドキュメントは、共同作業者が Cursor を使ってこのプロジェクトを編集できるようにするための手順書です。

---

## 📋 目次

1. [必要なツールのインストール](#1-必要なツールのインストール)
2. [GitHubリポジトリのクローン](#2-githubリポジトリのクローン)
3. [Cursorでプロジェクトを開く](#3-cursorでプロジェクトを開く)
4. [ローカルでの確認方法](#4-ローカルでの確認方法)
5. [変更をGitHub・Herokuに反映する方法](#5-変更をgithubherokuに反映する方法)
6. [トラブルシューティング](#6-トラブルシューティング)

---

## 1. 必要なツールのインストール

### 1-1. Cursorのインストール

1. **Cursor公式サイト**にアクセス
   - URL: https://cursor.sh/
2. 「Download」ボタンをクリック
3. ダウンロードしたインストーラーを実行
4. インストールが完了したらCursorを起動

### 1-2. Gitのインストール（まだの場合）

1. **Git for Windows**をダウンロード
   - URL: https://git-scm.com/download/win
2. インストーラーを実行（基本的にデフォルト設定でOK）
3. インストール完了後、コマンドプロンプトで確認：
   ```bash
   git --version
   ```

### 1-3. Node.js のインストール（ローカル確認用）

1. **Node.js公式サイト**にアクセス
   - URL: https://nodejs.org/
2. 「LTS（推奨版）」をダウンロード
3. インストーラーを実行
4. インストール完了後、コマンドプロンプトで確認：
   ```bash
   node --version
   npm --version
   ```

---

## 2. GitHubリポジトリのクローン

### 2-1. GitHubアカウントの準備

1. GitHubアカウントを作成（持っていない場合）
   - URL: https://github.com/
2. プロジェクトオーナーから**コラボレーター招待**を受け取る
3. 招待メールから承認

### 2-2. リポジトリのクローン

**方法1: Cursorから直接クローン（推奨）**

1. Cursorを起動
2. `Ctrl + Shift + P` でコマンドパレットを開く
3. 「Git: Clone」と入力して選択
4. リポジトリのURLを入力：
   ```
   https://github.com/中田-516/yuki_juku_lp202602.git
   ```
5. クローン先のフォルダを選択（例：`C:\Users\あなたの名前\Documents\Projects\`）
6. 「Open」をクリックしてプロジェクトを開く

**方法2: コマンドラインからクローン**

1. 作業したい場所にフォルダを作る（例：`C:\Projects`）
2. コマンドプロンプトまたはPowerShellを開く
3. 以下のコマンドを実行：
   ```bash
   cd C:\Projects
   git clone https://github.com/中田-516/yuki_juku_lp202602.git
   cd yuki_juku_lp202602
   ```
4. Cursorでこのフォルダを開く
   - `File` → `Open Folder` → クローンしたフォルダを選択

---

## 3. Cursorでプロジェクトを開く

### 3-1. プロジェクトを開く

1. Cursorを起動
2. `File` → `Open Folder`
3. クローンした `yuki_juku_lp202602` フォルダを選択
4. 「このフォルダを信頼しますか？」→ **はい**

### 3-2. ファイル構造の確認

プロジェクトのファイル構造：

```
yuki_juku_lp202602/
├── index.html              ← メインのHTMLファイル（主にこれを編集）
├── images/                 ← 画像ファイル
├── package.json            ← プロジェクト設定
├── Procfile                ← Heroku設定
├── .gitignore              ← Git除外設定
├── git-push.bat            ← GitHubプッシュ用
├── heroku-deploy.bat       ← Herokuデプロイ用
└── 共同作業者向け手順書.md ← このファイル
```

---

## 4. ローカルでの確認方法

変更を加えた後、Herokuにプッシュする前にローカルで確認しましょう。

### 4-1. 依存関係のインストール

**初回のみ実行**

1. Cursorのターミナルを開く（`` Ctrl + ` `` または `View` → `Terminal`）
2. 以下のコマンドを実行：
   ```bash
   npm install
   ```

### 4-2. ローカルサーバーの起動

1. ターミナルで以下を実行：
   ```bash
   npm start
   ```
2. ブラウザで以下のURLを開く：
   ```
   http://localhost:3000
   ```
3. 変更を確認

**サーバーの停止方法**
- ターミナルで `Ctrl + C` を押す

**または、バッチファイルを使う方法**
- `start-server.bat` をダブルクリック
- ブラウザが自動で開きます

---

## 5. 変更をGitHub・Herokuに反映する方法

### 5-1. 変更の確認

1. ファイルを編集・保存
2. Cursorの左サイドバーの「Source Control」（Git）アイコンをクリック
3. 変更されたファイルが表示される

### 5-2. 変更をコミット

**Cursorから（推奨）**

1. 「Source Control」パネルで変更内容を確認
2. 上部のメッセージ欄に**コミットメッセージ**を入力
   - 例：「バッジを削除」「見出しのサイズを調整」
3. `✓`（チェックマーク）ボタンをクリック
4. 変更がコミットされる

**コマンドラインから**

```bash
git add .
git commit -m "変更内容の説明"
```

### 5-3. GitHubにプッシュ

**Cursorから（推奨）**

1. 「Source Control」パネルの `⋯`（三点メニュー）をクリック
2. `Push` を選択

**コマンドラインから**

```bash
git push origin main
```

**またはバッチファイルを使う**

- `git-push.bat` をダブルクリック
- コミットメッセージを入力してEnter

### 5-4. Herokuにデプロイ

**方法1: バッチファイルを使う（最も簡単）**

1. `heroku-deploy.bat` をダブルクリック
2. 自動的にHerokuにデプロイされる
3. 完了したら https://yuki-9dbfd2b96a4e.herokuapp.com/ で確認

**方法2: コマンドラインから**

```bash
git push heroku main
```

---

## 6. トラブルシューティング

### Q1. `git push` で「認証エラー」が出る

**原因**: GitHubの認証が必要

**解決方法**:
1. GitHub Personal Access Token（PAT）を作成
   - https://github.com/settings/tokens
   - `repo` のすべての権限をチェック
2. トークンをコピー
3. Pushするときにパスワードとしてトークンを入力

### Q2. `heroku push` で「ログインが必要」と出る

**原因**: Heroku CLIのログインが必要

**解決方法**:
1. Heroku CLIをインストール（まだの場合）
   - https://devcenter.heroku.com/articles/heroku-cli
2. ログインコマンドを実行：
   ```bash
   heroku login
   ```
3. ブラウザが開くので、Herokuアカウントでログイン

### Q3. `npm start` でエラーが出る

**原因**: 依存関係がインストールされていない

**解決方法**:
```bash
npm install
```

### Q4. 変更が反映されない

**原因**: ブラウザのキャッシュ

**解決方法**:
- `Ctrl + F5` でスーパーリロード（キャッシュクリア）

### Q5. 「path not found」エラーが出る

**原因**: 日本語や特殊文字を含むパスが原因の可能性

**解決方法**:
- プロジェクトを英語名のフォルダに移動
  - 例：`C:\Projects\yuki_juku_lp\`

### Q6. クローンしたプロジェクトが空（ファイルがない）

**原因**: クローンが不完全、または間違ったリポジトリをクローンした

**解決方法**:

**方法1: 正しいURLで再クローン**

1. 既存のフォルダを削除または名前変更
2. Cursorで `Ctrl + Shift + P` → 「Git: Clone」
3. 正しいURLを入力：
   ```
   https://github.com/中田-516/yuki_juku_lp202602.git
   ```
4. クローン完了後、以下のファイルがあるか確認：
   - `index.html`
   - `package.json`
   - `Procfile`
   - `images/` フォルダ

**方法2: コマンドラインから再クローン**

```bash
# 安全な場所に移動（例）
cd C:\Projects

# クローン実行
git clone https://github.com/中田-516/yuki_juku_lp202602.git

# フォルダに移動
cd yuki_juku_lp202602

# ファイル一覧を確認
dir
```

**方法3: リポジトリへのアクセス権を確認**

- GitHubリポジトリのコラボレーターに追加されているか確認
- プロジェクトオーナーに招待を送ってもらう
- 招待メールから承認する

**クローン成功の確認方法**:

Cursorでフォルダを開いたときに、以下のファイルが見えればOK：
- ✅ `index.html`（メインファイル）
- ✅ `package.json`（プロジェクト設定）
- ✅ `Procfile`（Heroku設定）
- ✅ `images/`（画像フォルダ）
- ✅ `.git/`（非表示フォルダ）

---

## 📞 サポート

わからないことがあれば、プロジェクトオーナーに連絡してください：
- 電話：080-9106-8651 / 050-6877-5820

---

## 🎯 作業の基本フロー

```
1. プロジェクトを開く（Cursor）
   ↓
2. 最新版を取得（git pull origin main）
   ↓
3. ファイルを編集
   ↓
4. ローカルで確認（npm start）
   ↓
5. 変更をコミット（git commit）
   ↓
6. GitHubにプッシュ（git push origin main）
   ↓
7. Herokuにデプロイ（git push heroku main）
   ↓
8. 本番環境で確認（https://yuki-9dbfd2b96a4e.herokuapp.com/）
```

---

**更新日**: 2026年2月16日
**プロジェクト**: 結城塾 ランディングページ
**本番URL**: https://yuki-9dbfd2b96a4e.herokuapp.com/
